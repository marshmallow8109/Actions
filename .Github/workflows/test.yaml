name: gitacotions-test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow-dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: hello world
        run: echo "hello world"
        shell: bash
